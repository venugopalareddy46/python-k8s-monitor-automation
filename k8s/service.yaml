apiVersion: v1
kind: Service

metadata:
  name: python-automation-service

spec:
  selector:
    app: python-automation

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080

  type: ClusterIP
